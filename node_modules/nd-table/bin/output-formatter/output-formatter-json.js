"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.outputFormatterJSON = void 0;
function outputFormatterJSON(renderData, options) {
    var cellValue = renderData.cellValue, columns = renderData.columns, rows = renderData.rows, startX = renderData.startX, startY = renderData.startY;
    var out = [];
    for (var y = startY; y < rows; y++) {
        var row = [];
        for (var x = startX; x < columns; x++) {
            row.push(cellValue[x][y]);
        }
        out.push(row);
    }
    return JSON.stringify(out, null, (options === null || options === void 0 ? void 0 : options.compact) ? undefined : 2);
}
exports.outputFormatterJSON = outputFormatterJSON;
